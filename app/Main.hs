module Main where

import Protolude
-- import Lib
--import RulesInv
import Terms


main :: IO ()
main = undefined

-- main :: IO ()
-- main = do
    -- a <- getArgs
    -- case a of
    --     (i:n:_) ->
    --         let { iters = map fst $ headMay $ reads i :: Maybe Int ; 
    --               nseed = map fst $ headMay $ reads n :: Maybe Int } in
    --         case (iters, nseed) of
    --             (Just iters', Just nseed') -> test iters' nseed'
    --             _ -> print ("could not parse the given arguments" :: Text)
    --     _ -> print ("error wrong no. of arguments" :: Text)


